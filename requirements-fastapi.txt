# FastAPI Backend Requirements for Task 14
# Install with: pip install -r requirements-fastapi.txt

# Core FastAPI and ASGI
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
websockets>=11.0.0

# Database ORM and Migration
sqlalchemy>=2.0.0
alembic>=1.11.0
psycopg2-binary>=2.9.0
asyncpg>=0.29.0

# Data Validation and Serialization
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP Client for external services
httpx>=0.24.0
requests>=2.31.0

# Kafka Integration
kafka-python>=2.0.2
confluent-kafka>=2.1.0

# HDFS Integration
# Use 'hdfs' (WebHDFS client) instead of deprecated hdfs3 (libhdfs3)
hdfs>=2.7.0
pyarrow>=12.0.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Logging and Monitoring
loguru>=0.7.0
prometheus-client>=0.17.0

# Development and Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0
locust>=2.15.0
coverage>=7.2.0

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt<4.0.0  # Pin to 3.x for passlib compatibility
python-multipart>=0.0.6
slowapi>=0.1.9

# Caching
redis>=4.6.0

# Configuration management
python-dotenv>=1.0.0

# CORS is built-in via FastAPI/Starlette middleware; no separate package required

# Background tasks
celery>=5.3.0

# Time handling
python-dateutil>=2.8.2
pytz>=2023.3